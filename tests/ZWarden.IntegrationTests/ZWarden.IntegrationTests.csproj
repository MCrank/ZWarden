<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tier-2 (opt-in / networked) integration tests. Every test here needs a real
    PostgreSQL container, so CI tiers this WHOLE ASSEMBLY into the networked leg
    (ADR 0002: tiers split by assembly, categories slice within one). It never runs
    in the offline tier.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <LangVersion>14.0</LangVersion>
    <TestingPlatformCommandLineArguments>--minimum-expected-tests 20</TestingPlatformCommandLineArguments>
  </PropertyGroup>

  <!--
    The F32 Caddy reference-deployment test stands up a stub upstream in-process (Kestrel) behind the real
    caddy:2.11.4 container, so it needs the ASP.NET shared framework. No production project here depends on it.
  -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="TUnit" />
    <PackageReference Include="Testcontainers.PostgreSql" />
    <PackageReference Include="Testcontainers" />
    <PackageReference Include="Npgsql" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ZWarden.TestSupport\ZWarden.TestSupport.csproj" />
    <!-- The Postgres migration history, so MigrateAsync finds the networked provider's migrations. -->
    <ProjectReference Include="..\..\src\ZWarden.Migrations.Postgres\ZWarden.Migrations.Postgres.csproj" />
    <!-- The Agent's Docker runtime under test (F13). -->
    <ProjectReference Include="..\..\src\ZWarden.Agent\ZWarden.Agent.csproj" />
  </ItemGroup>

</Project>
