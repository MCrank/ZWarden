{
  "Serilog": {
    "Using": [ "Serilog.Sinks.Console", "Serilog.Sinks.File" ],
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/agent-.log",
          "rollingInterval": "Day"
        }
      }
    ],
    "Enrich": [ "FromLogContext" ]
  },
  "Agent": {
    "//ControlPlaneUri": "The control plane. F9 uses it for the one-shot enrollment POST; F10 for the SignalR transport.",
    "ControlPlaneUri": "https://localhost:8443",
    "//TrustFilePath": "Where the enrolled Agent stores its credential (F9). Defaults under %LOCALAPPDATA%/ZWarden/Agent/agent-trust.json.",
    "//EnrollmentSecret": "The one-time enrollment secret (F9). A SECRET: supply via env (Agent__EnrollmentSecret), never commit it. Consumed once on first run.",
    "HeartbeatInterval": "00:00:30",
    "HealthReportInterval": "00:00:15",
    "ShutdownTimeout": "00:00:10"
  }
}
