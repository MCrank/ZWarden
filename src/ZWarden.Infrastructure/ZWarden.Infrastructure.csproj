<Project Sdk="Microsoft.NET.Sdk">

  <!-- TargetFramework, Nullable, ImplicitUsings come from Directory.Build.props. -->

  <ItemGroup>
    <ProjectReference Include="..\ZWarden.Application\ZWarden.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- EF Core value converters for the typed ids (ADR 0004). Domain stays EF-free (arch rule 2),
         so the converters live here; F2 applies them in OnModelCreating. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
  </ItemGroup>

</Project>
