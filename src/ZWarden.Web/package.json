{
  "name": "zwarden-web",
  "private": true,
  "version": "0.0.0",
  "description": "Tailwind CSS build for ZWarden.Web. The generated wwwroot/app.css is committed and diffed by CI (ADR 0003).",
  "scripts": {
    "build:css": "tailwindcss -i ./Styles/app.tailwind.css -o ./wwwroot/app.css"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
